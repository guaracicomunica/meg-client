export type RankingCardType = {
  name: string;
  xp: number;
  avatar?: string;
}

export type RankingType = {
  name: string;
  xp: number;
  avatar?: string;
  level?: {
    name: string;
    avatar: string;
  }
}